import React from 'react';
import renderer from 'react-test-renderer';

// Simple smoke test
describe('Application Smoke Test', () => {
  it('has 1 equal to 1', () => {
    expect(1).toBe(1);
  });
});

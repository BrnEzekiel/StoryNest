{
  "name": "new",
  "version": "1.0.0",
  "description": "",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "backend": "npm run dev --workspace=apps/backend",
    "mobile": "npm run start --workspace=apps/mobile",
    "build:android": "cd apps/mobile && eas build -p android --profile preview",
    "build:apk": "cd apps/mobile && eas build -p android --profile preview",
    "build:aab": "cd apps/mobile && eas build -p android --profile production",
    "build:apk:local": "cd apps/mobile && eas build -p android --profile preview --local",
    "build:ios": "cd apps/mobile && eas build -p ios --profile production",
    "db:push": "prisma db push --schema=packages/db/prisma/schema.prisma",
    "db:seed": "node packages/db/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eas-cli": "^23.0.0",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}

import * as Haptics from 'expo-haptics';
import { Platform } from 'react-native';

/**
 * StoryNest Haptic Feedback Utility (Disabled by request)
 */
export const Impact = {
    light: () => {},
    medium: () => {},
    heavy: () => {}
};

export const Notification = {
    success: () => {},
    warning: () => {},
    error: () => {}
};

export const Selection = () => {};
